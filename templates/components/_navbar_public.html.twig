{# Public Navbar — Editorial Fashion Redesign #}
<nav class="landing-nav" id="landing-nav">
    <div class="landing-nav__inner">
        {# Logo #}
        <a href="{{ path('app_landing') }}" class="landing-nav__logo">
            <i class="bi bi-asterisk landing-nav__logo-icon"></i>
            OutfitShare
        </a>

        {# Desktop Links #}
        <div class="landing-nav__links">
            <a href="#features" class="landing-nav__link">Cómo funciona</a>
            <a href="#community" class="landing-nav__link">Comunidad</a>
            <a href="#about" class="landing-nav__link">Nuestra historia</a>
        </div>

        {# Auth Buttons #}
        <div class="landing-nav__auth">
            <a href="{{ path('app_login') }}" class="landing-nav__login">Login</a>
            <a href="{{ path('app_register') }}" class="landing-nav__signup">Empieza gratis</a>
        </div>

        {# Mobile toggle #}
        <button class="landing-nav__toggle" aria-label="Abrir navegación">
            <i class="bi bi-list"></i>
        </button>
    </div>
</nav>
