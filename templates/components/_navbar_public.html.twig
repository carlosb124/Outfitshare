{# Public Navbar (Whering Style - Transparent) #}
<nav class="absolute top-0 left-0 w-full z-50 py-6">
    <div class="container mx-auto px-6 flex justify-between items-center">
        {# Logo #}
        <a href="{{ path('app_landing') }}" class="text-2xl font-extrabold tracking-tighter flex items-center gap-2 group">
            <div class="w-10 h-10 bg-neon-lime text-black rounded-full flex items-center justify-center transform group-hover:rotate-12 transition-transform">
                <i class="bi bi-asterisk"></i>
            </div>
            OutfitShare
        </a>

        {# Desktop Links #}
        <div class="hidden md:flex items-center gap-8 font-bold text-sm">
            <a href="#features" class="hover:text-neon-lime transition-colors">Features</a>
            <a href="#community" class="hover:text-neon-lime transition-colors">Community</a>
            <a href="#about" class="hover:text-neon-lime transition-colors">About Us</a>
        </div>

        {# Auth Buttons #}
        <div class="flex items-center gap-4">
            <a href="{{ path('app_login') }}" class="font-bold hover:text-neon-lime transition-colors">Login</a>
            <a href="{{ path('app_register') }}" class="bg-neon-lime text-black px-6 py-2.5 rounded-full font-bold hover:scale-105 transition-transform shadow-[4px_4px_0px_0px_rgba(0,0,0,1)]">
                Sign Up
            </a>
        </div>
    </div>
</nav>
